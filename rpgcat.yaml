Model::EMKit:
    from_email: rpgcat@tree.bocks.com

Model::DB:
    connect_info:
        dsn: dbi:mysql:database=__ENV(MYSQL_DATABASE)__;host=__ENV(MYSQL_HOST)__
        user: __ENV(MYSQL_USER)__
        password: __ENV(MYSQL_PWD)__

Model::Hashids:
    args:
        salt: Change this value
